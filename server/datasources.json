{
  "mongodb": {
    "host": "localhost",
    "port": 0,
    "url": "",
    "database": "${MONGO_DATABASE}",
    "password": "${MONGO_PASS}",
    "name": "mongodb",
    "user": "${MONGO_USER}",
    "connector": "mongodb"
  }
}
